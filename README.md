# Docker Archlinux CUDA

Installed Versions:
* NVIDIA Driver 375.20 (manual install)
* CUDA 8.0.44 (as provided by Archlinux)

**IMPORTANT**: Host driver version must match exactly

## Usage
Start using docker-compose:
```
docker-compose up
```


